import React from 'react';
import MovieInfo from 'components/MovieInfo/MovieInfo';

const MovieView = () => {
    return (
        <div>
            <MovieInfo/>
        </div>
    );
};

export default MovieView;